fun maxSlidingWindow(nums,k){
  let n = arrayLength(nums)
  let deque = []
  let result = []
  for( i in 0 -> n ){
       if(arrayLength(deque)==0){
		    arrayInsert(deque,i)
		  }
  }
}

let result = maxSlidingWindow([1,3,-1,-3,5,3,6,7],3)
printOut(result)
